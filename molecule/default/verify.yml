---
- name: Verify
  hosts: all
  become: true

  tasks:
    - name: install bash
      ansible.builtin.package:
        name: bash
        state: present

    - name: try az
      ansible.builtin.command:
        cmd: az --version
      changed_when: false
